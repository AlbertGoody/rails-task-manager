<h1>All tasks</h1>
<% @tasks.each do |task| %>
<h5> <%= link_to task.title, task_path(task) %>  </h5>
<i class ="<%= task.completed ? 'far fa-check-square' : 'far fa-square' %>" > </i>
<%# <h5> <%= task.completed %> </h5>
<% end %>

<h3> <%= link_to "Create a task", new_task_path %> </h3>
